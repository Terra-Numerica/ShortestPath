import { RandomCopsStrategy } from './random-cops-strategy';

describe('RandomCopsStrategy', () => {
  it('should create an instance', () => {
    expect(new RandomCopsStrategy()).toBeTruthy();
  });
});
