import { OneCopsWinStrategy } from './one-cops-win-strategy';

describe('OneCopsWinStrategy', () => {
  it('should create an instance', () => {
    expect(new OneCopsWinStrategy()).toBeTruthy();
  });
});
