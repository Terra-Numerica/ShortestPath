import { PawnStateWaitingTurn } from './pawn-state-waiting-turn';

describe('PawnStateWaitingTurn', () => {
  it('should create an instance', () => {
    expect(new PawnStateWaitingTurn()).toBeTruthy();
  });
});
